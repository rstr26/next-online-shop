import React from 'react'
import { ReactChild } from '../_shared/types'

const AboutLayout = ({ children }: ReactChild) => {
    return (
        <div>
            {children}
        </div>
    )
}

export default AboutLayout