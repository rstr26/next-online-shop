import { ReactChild } from '@/app/shared/types'
import React from 'react'

const CheckoutStatusLayout = ({children}: ReactChild) => {
    return (
        <div>
            {children}
        </div>
    )
}

export default CheckoutStatusLayout