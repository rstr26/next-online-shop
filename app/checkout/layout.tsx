import React from 'react'
import { ReactChild } from '../shared/types'

const CheckoutLayout = ({children}: ReactChild) => {
    return (
        <div>
            {children}
        </div>
    )
}

export default CheckoutLayout